// SPDX-License-Identifier: BUSL-1.1
pragma solidity 0.8.20;

//import {NonblockingLzApp} from "@layerzerolabs/contracts/lzApp/mocks/LZEndpointMock.sol";
/*
contract LayerZeroRouter  is LZEndpointMock {
    constructor(uint16 chainId) LZEndpointMock(chainId) {}

    function call(address destination, bytes memory data) external {
        (bool success,) = destination.call(data);
        assert(success);
    }
}
*/
